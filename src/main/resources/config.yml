debug: false

messages:
  player_join: '&e%player% joined the game'
  player_quit: '&e%player% left the game'

instance:
  id: '1'
  name: 'instance1'

connection: 
  type: 'redis'
  redis:
    url: 'redis://<user>:<pass>@<host>:<port>/'
